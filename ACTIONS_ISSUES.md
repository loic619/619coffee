# GitHub Actions — open issues log

**Repo:** `loic619/619coffee` · **Opened:** 2026-09-01 · **Last reviewed:** 2026-09-04 (main is green *now*, but two direct pushes to `main` broke 9.1 CI Tests and 9.2 Backend Lint between 04:04 and 06:16 UTC this morning — **A7**) · **Last fix-queue run:** 2026-09-04 (C5 — retry + partial-write in the activity collector; PR open, not yet merged. Previous run 06:19 UTC: C4 — PR **#823** merged; the entry marking C4 `[x]` on 3 Sep was itself wrong, see the row)
**Evidence window:** 2026-08-25 → 2026-09-04 06:40 UTC. Sources: live Actions API (per-workflow listings, run logs for every failure in the window), `health.json`, and the three gates run directly against `main` at `b2e4f0c`. **The 7-day run record is usable but stale** — `workflow_activity.json` reads `generated_at: 2026-09-03T11:09:29Z`, ~19.5 h old. That is not a fresh freeze (**C5**'s specific failure mode): run 12 of 0.17 succeeded and produced this file; it is simply late, the original problem **C4** just fixed. C4's fix cannot be observed live yet — it triggers off workflow 1.4 finishing, and 1.4 has not completed since the merge landed 20 minutes before this review.

> **How this file works.** Two Claude Code **Routines** keep it current — they
> run on Anthropic's cloud, not in this repo, so there is no workflow here to
> look for. **619coffee — Actions health watch** (daily 13:30 Asia/Saigon) reviews
> what the Actions actually did, ticks off what it can verify, appends new
> findings, updates the *Last reviewed* date, and commits the result to `main`.
> So we never re-derive the same problem twice.
>
> **Status key:** `[ ]` open · `[~]` partly fixed, root cause still open · `[x]` done · `[?]` watch, not yet a problem
>
> Priority is **P1** (silently loses or corrupts data) · **P2** (weakens a safety net) · **P3** (housekeeping).
>
> **This file is also a work queue.** The second routine, **619coffee — Actions
> fix queue** (weekdays 14:30 Asia/Saigon), takes the highest-priority open item,
> fixes it on a branch, ticks the box in the same commit, and opens one pull
> request labelled `auto-fix`. It will not start another until that PR is closed
> or merged — your review sets the pace. Items marked ***(human-only)*** are
> skipped: they need repo settings or a decision from you, which no agent should
> make on your behalf.

---

## Next up

*Rebuilt by the watch routine on every run — if this disagrees with the tables below, the tables win.*

| | Issue | Why it's next |
|---|-------|---------------|
| **In flight** | — none | C5's `auto-fix` PR (**#833**) merged this run. The queue is unblocked and should pick up **A2** next. **Dispatch 0.17 once by hand** to rebuild the record immediately rather than waiting for the next trigger. |
| **watching** | **B8** · P1 | **Symptom cleared 4 Sep; cause understood; mitigation unproven.** The feed is serving and the data is current (`2026-09-03` both markets). Six PRs landed — the diagnosis is that ICE blocks per RUNNER IP, driven by our own request volume, and a healthy run now issues ~186 fewer requests. What is NOT established is that the reduction is enough: the run that recovered drew a clean IP, so it shows the feed works, not that the blocking stops. The next few days of scheduled runs decide it. Nothing to do until then. |
| **1st** | **A2** · P2 | Pin ruff. Re-confirmed still unpinned today. |
| **2nd** | **A5** · P2 | Make the smart-quote guard fail loudly when its detector breaks. Re-confirmed still the fragile `if grep -rnP …` construct today. |
| **3rd** | **B2** · P2 | Let the sentinel dispatch its own scraper. |
| **4th** | **B7** · P2 | Nothing watches `data_asof`, so "scraper ran fine, content frozen for months" has no alarm at all. |
| **5th** | **C2** · P2 | Count the rescue dispatches. |
| then | **B6 · D1 · D2 · D3** · P3 | Housekeeping. |

**Waiting on you — the agent will never take these:** **A1** (branch protection; still the highest-value item in this file — **A6** and now **A7** are its fourth and fifth instances in eleven days) · **C1** (pick a polling strategy).

**Not in the queue:** A3, A4, A6, A7, B1, B4, B5, C3, C4, E1, E3 closed · B3, E2, E4 watching.

**Note on B8's status.** It is `[~]`, not `[ ]`, and the distinction matters: the
part that could lose data unnoticed is closed, so this is no longer an
*invisible* failure — it is a visible outage waiting on a diagnosis. If ICE
lifts the block on its own, B8 closes with no code change and the two guards
stay as the thing that would have caught it.

**Correction, 2026-09-04 — the block is per RUNNER IP, and it did not start on
3 Sep.** Two claims in the first version of this row were wrong, both from
comparing runs that differed in more than one variable:

* *"Began 3 Sep."* No — run `33592853150` (2 Sep 04:58) was already returning
  403 on 877 of the 900 log lines sampled. It then timed out at two hours.
* *"ICE serves the archive and refuses recent files."* That came from a smoke
  run returning 10/10 at 08:42 while a backfill 403'd at 08:45 — but those were
  **separate jobs on different runners** (`1000023630` vs `1000023637`), so
  archive-vs-live and clean-IP-vs-blocked-IP were confounded and the wrong one
  was named.

What the evidence actually supports: run `33599302408` started at **06:59:12**
on 2 Sep, the same second the blocked run was killed at 06:59:05 — a fresh
runner, a different IP — and immediately got normal 404s where the previous IP
had got nothing but 403s. Every run is **all-or-nothing**: no run mixes 200s
and 403s. That is the signature of a per-source-IP block, not a URL-scope rule.

It also explains the "recovery" pattern in the run history: repeated triggers
appearing to fix themselves are not the block lifting, they are rolling a new
runner IP until one is not blocked.

The experiment that settles it is a smoke run under #838's code, which probes
an archive URL and a live URL **in the same job** and therefore on one IP. If
both pass, the archive/live theory is dead and it is purely per-IP.

**Result (run 33866182291, runner `1000023667`):** both passed.

```
=== SMOKE: 10/10 OK (archive URLs) ===
=== SMOKE (live, 2026-09-03): ✓ HTTP 200 ===
```

Archive-vs-live is dead. The block is per source IP and file age is irrelevant.

---

**End of 4 Sep. Where B8 stands.**

*Symptom* — gone. Backfill `33864176535` landed `cd7a3a04`: arabica and robusta
both at `2026-09-03`, found at sweep candidate 280 exactly as the arithmetic
predicted. The feed is serving.

*Cause* — understood, and it is us. ICE blocks a runner IP after sustained
volume against `/marketdata/publicdocs/`. The telemetry shows how we earned it:
1,178 requests on 31 Aug, 1,976 on 1 Sep, 2,022 on 3 Sep, ~99% of them misses.
The block began mid-run on 3 Sep, roughly 187 requests into that day's sweep.

*Mitigation* — merged, six PRs:

| | |
|---|---|
| #834 | a run that asks ≥10 times and gets nothing fails, instead of writing unchanged files and exiting 0 |
| #835 | 8 consecutive 403s abort instead of walking 1,920 candidates |
| #838 | the abort stops the CLOCK too (it was still sleeping 5s per skipped call); a block is not retried; smoke can finally see a live block; an in-flight run no longer spawns duplicates |
| #839 | tier-1 stops re-asking questions the sweep or a previous run already answered — ~150 GETs |
| #840 | per-day sources fetch one day, not seven — 42 GETs to 6 |
| #843 | a 403 skips the SECTION, not the rest of the run |

A healthy run now issues roughly **186 fewer requests**.

*What is still open, and why this row is not `[x]`* — the reduction is
unverified in production. The run that recovered drew a clean runner IP, which
demonstrates the feed works, not that we have stopped earning blocks. If the
scheduled runs over the next few days stay green, close it. If a 403 storm
recurs, the run will now say so in seconds instead of two silent hours, and the
next lever is the tier-2 sweep itself — 1,870 requests to locate one file is
still the single largest source of volume in the system, and nothing merged
today touched it.

*One process note.* #843 fixed a hazard introduced by #835 earlier the same
day: the 403 bail-out set the run-wide flag, so a storm on the last source
would silently truncate an otherwise good run — and the wholly-refused guard
would not fire, because earlier fetches had succeeded. That is the third
variant of "reports success while collecting nothing" found in one session.
The shape is worth watching for specifically.

---

## A. CI gates — the checks that are supposed to stop bad code

**Main is green right now, and each detector was positive-controlled before its result was believed.** All three gates were run against `main` at `b2e4f0c` (2026-09-04 06:40 UTC): **ruff 0.15.11** reports *All checks passed* on `backend/` (the sandbox's default `ruff` on PATH resolves to 0.15.8 and shadows the pinned install — `/root/.local/bin/ruff`/`/usr/local/bin/ruff` had to be invoked explicitly to get 0.15.11; both agree today, but that shadowing is worth knowing about for next time). **pytest** — the full suite CI runs (`tests/ scraper/tests/`) — reports **1422 passed**, executed here, not inferred from a run badge. The **smart-quote** scan is clean under `LC_ALL=C.UTF-8 grep -rnP …`, the exact CI invocation, positive-controlled first against a planted smart quote in a throwaway file (which it caught, exit 0) before being trusted on a clean repo (exit 1).

**But it was not green three hours ago.** See **A7**: two direct pushes to `main` (not through a PR) broke 9.1 CI Tests and 9.2 Backend Lint twice each between 04:04 and 06:16 UTC this morning, on top of **A6**'s test-gate outage two days prior.

| ID | Status | What it means for you (plain language) | Cause | Proposed fix |
|----|--------|----------------------------------------|-------|--------------|
| **A1** | `[ ]` **P1** *(human-only)* | Your gates go red and **nothing stops**. This has now happened **five times in eleven days**. Backend Lint was red for three days while **39 pull requests merged** over it (A3); the smart-quote guard was red for five days while **48 merged** (A4); the test gate was red for 3½ hours on 3 Sep (A6); and this morning it happened *twice more within two hours*, both from commits pushed straight to `main` with no PR at all (A7) — the fastest-turnaround instance yet, but also proof that even a same-morning self-fix depends on another session noticing, not on anything that is guaranteed to notice. The checks are smoke alarms with no one required to listen. | The workflows run on every PR, but they are not **required status checks** in branch protection on `main`. A red result is just a red icon — GitHub still lets the merge (or the direct push) through. | In GitHub → Settings → Branches → `main`, mark **9.1 CI Tests**, **9.2 Backend Lint** and **9.3 Smart-quote guard** as required checks. Five minutes, no code. This is the single highest-value item in this file: A3, A4, A6 and A7 are all symptoms of it. Required checks would not stop a direct push from landing red, but they would stop it from being trusted — the PR flow this repo already prefers (per `CLAUDE.md`) is the piece that actually prevents A7's shape. |
| **A2** | `[ ]` **P2** | A tool you don't control can turn your build red overnight, for reasons you never chose. | `lint-backend.yml` runs `pip install --quiet ruff` with **no version pin** (re-confirmed 3 Sep). Ruff ships new rules frequently; the next release can start flagging code that was fine yesterday. | Pin it: `pip install ruff==0.15.11`, and bump the version deliberately when you want the new rules. *(Verified 3 Sep: 0.15.11 passes clean on current `backend/`, so this fix can be applied exactly as written.)* |
| **A3** | `[x]` **P1** | *(Resolved 30 Aug)* Backend Lint failed 63 of 127 runs — not flaky, genuinely broken. | A single unsorted import block (`I001`) in `backend/scraper/probe_port_ids.py`, red on `main` from `bd481087` (27 Aug, #772) through `32862bb6` (29 Aug, #802). Every push run **and** every PR run failed on that one line, and 39 PRs merged over it. **On the introducing commit:** #799's message and a review both attribute it to #778, but the file was *created* by #772 (`git log --diff-filter=A`) already carrying `PORTS, _get, _START_YEAR`, and full-repo ruff flips PASS→FAIL[I001] exactly across `bd481087^ → bd481087`. #778 later reshaped that same line (adding `_HEADERS`); it did not introduce the violation. | Fixed incidentally in `ec1bc457` (#799, 30 Aug) — a one-line reorder folded into an unrelated PR. Closed. A1 is what stops it recurring. |
| **A4** | `[x]` **P1** | *(Resolved 31 Aug — corrected 2 Sep)* The smart-quote guard's 20 failures were **not** the guard doing its job on bad branches. A smart quote was sitting on `main` itself for five days, so the guard went red on innocent branches whose diffs never touched the file. **This is a second instance of A1**, not a non-event. | A typographic quote in `frontend/components/supply/SupplySDTab.tsx` landed with `1afb2660` (#763, 26 Aug) and stayed until `a98835ec` (#803, 31 Aug). Verified by scanning `main` at each commit across the window. | Fixed in #803. Closed. Verify with `git log -S'“' -- frontend/components/supply/SupplySDTab.tsx`. |
| **A5** | `[ ]` **P2** | Your smart-quote guard can break in a way that shows a **green tick**. If it ever stops working, it will not tell you — it will quietly report "no problems found" forever, and you would only discover it the way we discovered A4: weeks later, by accident. | `lint-smart-quotes.yml` runs `grep -rnP '[\x{2018}\x{2019}\x{201C}\x{201D}]' frontend/` (re-confirmed unchanged 3 Sep). When the shell's locale is not UTF-8, that pattern is not merely unmatched — `grep` **aborts** with *"character code point value in \x{} or \o{} is too large"* and exit code **2**. The step is written as `if grep …; then <fail>; fi`, which treats *any* non-zero exit as "clean" and prints `✓ No smart quotes found`. **Reproduced again on 3 Sep in this sandbox** (`LC_CTYPE=POSIX`): the exact CI command exited 2 against a file containing a real smart quote, and matched it correctly under `LC_ALL=C.UTF-8`. GitHub's current `ubuntu-latest` image sets a UTF-8 locale, so the guard genuinely works today — A4 proves it matched — but nothing in the workflow pins that, and a runner-image change would disarm it silently. | In `lint-smart-quotes.yml`: (1) set `LC_ALL: C.UTF-8` on the step's `env:`; (2) stop inferring the result from `if grep` — capture it explicitly (`set +e; grep -rnP … ; rc=$?; set -e`) and branch on `rc`: `0` = smart quotes found → fail, `1` = clean → pass, **anything else → fail with "detector error"**; (3) add a positive-control step *before* the scan that pipes a known smart quote into the same `grep` invocation and fails the job if it does not match. That last one is the point: a detector that cannot find a planted positive must not be allowed to report "clean". |
| **A6** | `[x]` **P1** | *(Opened and closed 3 Sep — 3½ hours red.)* Your test gate went red on `main` this morning and six pushes landed on top of it before it was fixed. Nothing was lost and it is green again, but it is the **third** time in ten days that a broken gate on `main` did not stop anything. Recording it because A1 is the fix and this is the evidence for A1, not because anything is outstanding. | Two workflows added on 3 Sep each took a workflow number already in use, which trips the uniqueness guard `test_workflow_numbers_are_unique` (added in #735 for exactly this): `probe-ice-expiry-calendar.yml` took **0.21**, already held by `backfill-weather-history.yml`; `threshold-alerts.yml` took **1.18**, already held by `model-health.yml`. Both new files are absent at `6b35290` and present now, so both are genuinely new today. 9.1 CI Tests on `main`: `e2b78476` 04:08 **fail**, `8b8a527c` 04:12 fail, `5e144367` 04:15 fail, `acc1a531` 04:20 fail, `846b50ac` 07:07 fail, `3fc27589` 07:17 fail — then green from `50bc9bab` 07:42. | Fixed in `50bc9ba` — *"ci: renumber the two new workflows — 0.21 and 1.18 were already taken"*. Verified here: the guard test passes on `bed0ae3` and the full suite is **1334 passed**. Closed, no action. Worth noting the guard did its job perfectly — it named both clashes and their files. The gap is only that a red gate does not block a merge (**A1**). |
| **A7** | `[x]` **P1** | *(Opened and closed 4 Sep — two hours red, twice.)* This morning `main` broke, was fixed, and broke again the same way within an hour — from commits pushed **straight to `main`, no PR** — while other work kept landing on top blind each time. No data was lost, and both breaks are fixed on the tip you'd read today, but it is now the **fourth and fifth** instances of A1 in eleven days, and the fastest pair yet. | Two separate direct pushes, both confirmed `head_branch: main`, `event: push` against the live API (not PR merges — no `(#nnn)` in either message): **(1)** `539b7fb1` (04:04) rewired `/brazil` and `/ecf` to a new data schema; 9.1 CI Tests failed with 7 tests red, including its *own* new tests (`test_brazil_reads_the_v2_cecafe_schema` asserted bags data, got "Brazil data unavailable"). Root cause (found by a later PR, not this review): a test-isolation leak in `telegram/data.py` — `DATA_DIR` was snapshotted at import time, one test's `importlib.reload` never got undone, and six unrelated handler tests then read a deleted tmp directory. **(2)** `dafa696` (05:27) rewrote `/brief`, `/quote` and `/cot`'s output format; 9.1 CI Tests failed with 15 tests red — old tests in `scraper/tests/test_brief_layout.py` and `telegram/tests/test_cot_handler.py` still pinned the pre-redesign text, and two functions the new tests expected (`_signal_line`, `_SEVERITY_TAG`) didn't exist yet in `telegram/handlers/cot`. A third, unrelated direct push, `641086b` (05:22), also broke 9.2 Backend Lint with a plain `I001` unsorted import in a new test file. | Both fixed same morning by follow-up commits that explicitly named the inherited red per `CLAUDE.md`'s own instruction ("fix it as its own commit, say so, and carry on"): PR **#830** fixed (1) at 04:31 (`cae0c89`, folding in the `DATA_DIR` fix and the stale footer assertion); a direct commit `0dfec60` (06:15) fixed (2) by updating the 15 stale assertions; `641086b`'s own lint error is clean on current `main`. Re-verified here: `ruff check .` on `b2e4f0c` is clean, `pytest` is 1422 passed. **This row needs no further fix beyond A1** — it exists purely as A1's freshest evidence, and to note that this time the failure mode was a bare push to `main`, not a red PR merging: even the PR-and-review flow `CLAUDE.md` mandates for everything except this file and `IMPROVEMENTS.md` was bypassed, twice, in the same morning. |

**Correction, 2 Sep.** The first version of this file asserted the opposite — that `main` stayed clean and the guard was working as designed, so "don't chase it." That was wrong, and it was wrong for a mechanical reason worth recording: the check used `grep -P '[\x{2018}...]'`, which **errors out** in this environment rather than matching, and the surrounding loop read a non-zero exit as "no hits". Every "clean" it reported was a false negative from a broken detector. Two lessons, both now standing rules for the watch routine: **positive-control any detector before trusting a negative result**, and treat "no action needed" rows as claims to verify, not conclusions — the errors in this file have lived in the dismissals, not the alarms. **Follow-up, same day:** the identical construct is in the shipped workflow, not just in the review script. That is now **A5**.

**Method note, 3 Sep.** The same lesson bit again, in a new place. This review's first freshness sweep scanned `health.json` for bare `YYYY-MM-DD` strings and reported the oldest source at 22 days. That was a broken detector: the stamps that matter most are full timestamps (`2026-07-01T00:00:00`), which the pattern silently skipped — including `ajca`, the one closest to its limit at 92%. Re-run against every stamp with the real thresholds parsed out of `check-scrapers-freshness.yml`, the answer changed. A scan that cannot see the rows it is meant to judge reports "all clear" just as confidently as one that can.

---

## B. Source detection — how new monthly data gets noticed

| ID | Status | What it means for you (plain language) | Cause | Proposed fix |
|----|--------|----------------------------------------|-------|--------------|
| **B1** | `[x]` **P1** | *(Resolved 2 Sep — confirmed by live sweep 3 Sep.)* The watcher that is supposed to notice when Brazil publishes can now read both sources it was blind to. It had never once succeeded on CONAB or MDIC Comex; it can now see both. | `1.17 – Source Sentinel` should spot publication day and trigger the scraper. For `conab` and `comex`, `data/source_sentinels.json` had shown `signal: null` since the file existed — the probe patterns never matched. Probes repaired in `backend/scripts/source_sentinel.py` (#818, commit `72dc888`): *CONAB* keyed on `.pdf`/`.xls` hrefs that gov.br's Plone site does not serve, and now keys on what `conab_supply.py` itself keys on; *Comex* raised `SSLError` on every probe because `balanca.economia.gov.br` serves an incomplete cert chain, and now matches the scraper's `verify=False` TLS posture. | **Verified, and here is how.** The confirmation criterion this row set was "watch the next sweep for `conab`/`comex` gaining a non-null `signal`". Walking the git history of `data/source_sentinels.json`: at `6b35290b` (2 Sep, immediately before the fix) both read `conab=null comex=null`; at `fe7ee1e0` and `a8ad4cfc` (2 Sep, the first sweeps after it) both read non-null — CONAB a content hash, Comex an ETag + `Last-Modified` pair. Both probes now reach their source. Closed. **One caveat, deliberately not reopened as an issue:** `last_found` is still null and `confirmed` still `2026-07` for both, which is expected — the fix baselined a signal, and `last_found` only stamps when that signal *changes*. End-to-end detection is therefore demonstrated as far as it can be until Brazil next publishes. `conab`'s `blind_alerted: "2026-09"` is a flag set before the fix; if the daily blind alert fires again after a successful read, that is a new bug and belongs in a new row. Safety-net crons (12th + 20th monthly) remain in place and the data is current either way. |
| **B2** | `[ ]` **P2** | When the sentinel *does* notice something is overdue, it writes a message asking a human to go and run the scraper by hand. That human is you, and it went unread for five days. | The sentinel raised `overdue_alerted` and `blind_alerted` for August and posted *"still undetected 21d past its window — the probe pattern may be broken; run the scraper manually"*. It has no path to actually dispatch the scraper itself. Still true on 3 Sep: `conab` and `comex` both carry `overdue_alerted: "2026-09"`. | Give the sentinel the same self-heal path `1.8` already uses: when a source is N days overdue, dispatch its scraper workflow directly instead of asking. The comment in `scraper-monthly-conab.yml` says it best — *"That instruction should not need a human."* |
| **B3** | `[?]` **P3** | Two Vietnam customs sources look half-detected — probably fine, worth one glance. | `vn_customs` and `vn_customs_dest` have a `signal` URL and `confirmed: "2026-08"` but `last_found: null`. Unchanged on 3 Sep. Note B1 established the benign reading of exactly this shape: `last_found` stamps on a signal *change*, not on a successful read, so a source baselined but not yet re-published sits here legitimately. | Confirm which. If by design, no action — and if B1's explanation covers it, close this rather than spending a PR. |
| **B4** | `[x]` **P2** | *(Working correctly)* `1.5 – Check Data Pipeline Freshness` failed 6 of 7 runs. | Not broken — **that workflow's job is to fail when data is stale.** I replayed it against each day's `health.json`: it fired 26–30 Aug on exactly the three B1 feeds, crossing their 45-day limit, and went green again on 31 Aug once they were refreshed. | No action. This one earned its keep. Treat its failures as messages, not incidents. |
| **B5** | `[x]` **P2** | *(Resolved 3 Sep.)* The missing ICE robusta report arrived. Both monthly ICE reports are now current and neither is near its limit — the 7 Sep alarm this row predicted will not fire. | `health.json` now reads `ice_robusta_age_allowance: 2026-08-31` alongside `ice_arabica_ageing: 2026-08-31`. At 3.3 days against a 38-day threshold that is 9% of limit, against 87% when this row was written. So the answer to the row's own either/or was **(a)** — ICE simply had not published the August age-allowance file yet, and one of the 2/3 Sep catch-window retries collected it. There was no regression in the "already captured this month?" guard, and `scraper-ice-monthly-reports.yml` **needs no change**. | Closed, verified against `health.json` rather than a run badge. The proposed diagnostic (read the 1.14 log to tell cause (a) from cause (b)) is **no longer needed** — the outcome settled it. Do not spend a PR on this row. |
| **B6** | `[ ]` **P3** | The Cecafe daily scraper failed 4 times in the window, and the error it prints about itself is **wrong**, which would send whoever fixes it chasing the wrong thing. No data was lost — the file stayed inside its 3-day guard and the runs have been green since 31 Aug. *(Recurred once more, 3 Sep 19:41 UTC — same bot-challenge shape, still no data lost: `cecafe_daily.json` was 1 day old against a 3-day alert threshold, and the job logged "Fetch failed but the data is still current — no alert." Root cause below is unchanged.)* | Failures on 26, 29 and 30 (×2) Aug. All three retry attempts in each run died identically: Cecafe served a bot-challenge interstitial (~7,100 chars) and the browser fallback raised `Page.content: Unable to retrieve content because the page is navigating and changing the content`. In `backend/scraper/fetch_cecafe_daily.py` the challenge-polling loop calls `await page.content()` at **line 132** while the Cloudflare challenge is still redirecting; Playwright raises instead of returning HTML, and the exception escapes the loop as `CecafeUnreachable`. The script then prints *"This is the recurring connect-timeout pattern"* — it is not a connect timeout, and nothing timed out. | Two small changes in `backend/scraper/fetch_cecafe_daily.py`: (1) wrap the in-loop `await page.content()` (line 132) in `try/except` and, on a navigation-in-progress error, treat it as *"challenge still up"* — sleep `poll_ms` and continue the loop instead of aborting the whole run; apply the same guard to the settle read at line 139, retrying once after `await page.wait_for_load_state("domcontentloaded")`. (2) Correct the message so it names the bot challenge rather than a connect timeout. |
| **B7** | `[ ]` **P2** | **A scraper can run green every single day while the data inside it hasn't moved for five months, and nothing anywhere would tell you.** Your freshness alarm watches whether the *scraper ran*, not whether it *brought anything back*. Right now nine feeds have a scraper stamp from this morning and content months old — the worst is Indonesian exports, whose newest figure is from **April**, 155 days ago. Some of those are perfectly normal publication lag and some may not be; the point of this row is that nothing is checking. | `check-scrapers-freshness.yml` reads only the `scrapers` (40 keys) and `exporters` (54 keys) blocks of `health.json`. The `data_asof` block — also 40 keys, and the one that records *how old the data itself is* — is never read: `grep data_asof .github/workflows/check-scrapers-freshness.yml` returns nothing. So `1.5` cannot see this class of failure at all, which is the same shape as the three silent failures #730 was written to catch. Measured 3 Sep, scraper stamp <3 days old but `data_asof` >60 days: `indonesia_exports` 155 d (2026-04), `honduras_exports` 94 d, `ethiopia_exports` 94 d, `enso` 94 d, `colombia_exports` 94 d, `vietnam_exports` 64 d, `us_cpi` 64 d, `retail_cpi` 64 d, `fertilizer_wb` 64 d. **Do not treat that list as nine bugs** — `us_cpi` and `retail_cpi` at `2026-07` are exactly right for early September (August CPI publishes mid-September), and several others are plausibly normal lag. `indonesia_exports` is the one that looks wrong on its face: `frontend/public/data/indonesia_exports.json` was rewritten on 2 Sep, so the scraper is running and writing — it is just not bringing back anything newer than April. | Two steps, in this order. **(1)** Add a `DATA_ASOF_THRESHOLDS_D` table to `check-scrapers-freshness.yml` and check the `data_asof` block against it in the same loop that already checks `scrapers`, with a generous per-source default (say 2× the source's scraper threshold) so it alarms on months, not days. Seed the table from the source's real publication cadence, not from today's ages — otherwise it enshrines whatever is currently broken as normal. **(2)** Separately, and *before* setting Indonesia's threshold, establish which of the nine are genuine: for each, compare `data_asof` against the source's actual publication schedule. That is a diagnosis, not a code change, and its result should be written back into this row. Start with `indonesia_exports`. |
| **B8** | `[~]` **P1** | **The certified-stocks feed is dark right now.** ICE has refused every request since roughly the evening of 3 Sep, so arabica is frozen at `2026-09-02` and robusta at `2026-09-01`. Until today a blocked run **reported success**: it fetched nothing, merged nothing, rewrote both JSONs unchanged and exited 0 — the only symptom was the data quietly ageing, with no alert. That half is fixed. The block itself is not, and no amount of retrying will clear it. | Every request to `www.ice.com/marketdata/publicdocs/` returns **HTTP 403 with an HTML challenge body** — Akamai serving a block page instead of the file. Run `33838897893` (4 Sep 05:00): **1,368 refusals in the last 71 minutes of log alone, zero 200s, zero 429s.** It is **not** rate limiting, and the instinctive remedy is the wrong one: the measured request gap was **3.05 s**, exactly the configured interval, with no throttle drift and no `Retry-After` anywhere — slowing to 4 s would only fail slower. It is also not an IP ban on GitHub runners: probe **0.30** fetched `www.ice.com/products/…` from a runner at **07:50 the same morning** and got 200 with 78 KB of real content. The block is scoped to the marketdata path. | **Fixed already — the two defects the outage exposed:** (1) a run that asks ≥10 times and gets **nothing** now fails non-zero instead of reporting success (#834), and 403 is counted and printed separately from 429 so the next reader sees which remedy applies; (2) eight consecutive 403s now **abort** the run (#835) — there was a bail-out for 429 and none at all for 403, so the tier-2 sweep walked all 1,920 candidates for 96 minutes and the job died on its 120-minute timeout, which GitHub reports as *"cancelled"* and is why it never looked like a failure. **Still open, and the actual issue:** determine what ICE now requires on that path — header/UA signature, `Referer`, a cookie, or a moved path. A probe that walks a few header variants from a runner would settle it in one dispatch, the way 0.30 settled the IP question. Until then the feed stays dark and the guards make that loud instead of silent. |

---

## C. Scheduling reliability — whether crons actually fire

| ID | Status | What it means for you (plain language) | Cause | Proposed fix |
|----|--------|----------------------------------------|-------|--------------|
| **C1** | `[ ]` **P1** *(human-only — pick an option first)* | Your live-quote poller is scheduled to run ~360 times a week. It actually got **45 scheduled runs — about 12%**. The feed only stayed alive because a rescue mechanism fired **241 emergency runs** to cover the gap. It works, but you are running on the backup generator full-time and the main power has been out for weeks. | GitHub Actions silently throttles high-frequency crons on this repo (documented twice before in `poll-acaphe-quotes.yml`: June and August). The `1.8` freshness checker compensates by dispatching the poller whenever quotes go stale. | Two options, pick one: **(a)** accept the rescue path as the primary mechanism and rewrite the cron down to a low, reliable heartbeat — stop pretending the 15-minute schedule works; **(b)** move the polling loop to a single long-running job (one run that sleeps and polls) instead of many short cron ticks, which GitHub throttles far less. Either way, the current setup hides its own failure. |
| **C2** | `[ ]` **P2** | That rescue mechanism is a single point of failure holding up your most time-sensitive feed, and if it breaks the failure is silent. | `check-live-quotes.yml` self-heals by calling the GitHub API (`actions/workflows/poll-acaphe-quotes.yml/dispatches`), which needs `actions:write`. If the token, permission or API call breaks, the poller drops to its real ~12% cron rate and nothing announces it. | Add a cheap tripwire: alert if scheduled-run count over 24h falls below a floor, or if `1.8` issues more than N rescues in a day. Right now nobody is counting the rescues. |
| **C3** | `[x]` **P3** | *(Closed 2 Sep — solved by rescheduling, not by code.)* The daily review used to read a run-record file that was ~19 hours old, so it could not see the previous afternoon. It now reads one that is twenty minutes old. | The 08:00 Cowork debrief this row was written for no longer exists. Its replacement, the **Actions health watch** routine, runs at 13:30 Asia/Saigon = 06:20 UTC — *after* workflow 0.17 rebuilds `workflow_activity.json` at 06:10 UTC. | No action. The proposed `00:40 UTC` cron on `build-workflow-activity.yml` is **not** needed; do not add it. *(Footnote, 3 Sep: the premise that this rebuild happens at 06:10 is false — see C4 — and on 3 Sep it had not happened for two days — see C5. The row stays closed because its proposed fix is still the wrong one, not because the staleness went away.)* |
| **C4** | `[x]` **P1** *(merged 4 Sep, 06:19 UTC — PR #823, commit `0393b3c`; see the 4 Sep correction below. C5 is the other half and is still open)* | **This one is about trusting this file.** C3 above was closed on the belief that the run record is rebuilt at 06:10 UTC, just before the daily review reads it. That belief is wrong: the rebuild almost never happens at 06:10. So when this log says "nothing failed", it is reporting on a window that may be missing the last day — and the worst incidents in this file (A3, A4) were both things that sat unnoticed. A quiet report is not currently proof of a quiet day. | `build-workflow-activity.yml` (workflow **0.17**) declares `cron: '10 6 * * *'`. Its actual scheduled start times: 26 Aug **07:06**, 27 Aug **17:36**, 28 Aug **18:37**, 29 Aug **12:39**, 30 Aug **11:51**, 31 Aug **13:42**, 1 Sep **11:35**, 2 Sep **11:13** — between 56 minutes and **12 h 28 m** late, never near 06:10. On 3 Sep there was still no run by 07:50. This is the same repo-wide throttling as C1, and it is already documented in the repo: `scraper-ice-certified-stocks.yml` carries the comment *"GH cron on this repo fires 4-5h LATE systematically (verified 2026-06-10 against run history)"*. It also explained B5's 1.14 firing at 17:40 against a 14:00 cron. | Stop depending on the cron's timing. In `build-workflow-activity.yml`, add an event trigger alongside the existing schedule: `workflow_run:` on `workflows: ["1.4 – Export and Publish Static JSON"]`, `types: [completed]`. 1.4 runs several times a day at the end of the data cycle, so the record is then never more than one export cycle old whenever the review reads it. Keep the cron as a fallback and keep the existing `concurrency` block. To stop it rebuilding six times a day, add a job-level guard that exits early when `workflow_activity.json`'s `generated_at` is under ~6 hours old — the run costs ~90 s, so the guard is about tidiness, not cost. **Do not** add the `00:40 UTC` cron that C3 proposed: the problem is that crons on this repo fire late, so a second cron inherits the same defect. **Fix this together with C5** — an event trigger that fires more often is worth little while any single API hiccup still aborts the whole build. **Done 3 Sep** (as proposed, no divergence): `build-workflow-activity.yml` gained `workflow_run: workflows: ["1.4 – Export and Publish Static JSON"], types: [completed]` alongside the unchanged `06:10` cron and `workflow_dispatch`; the `concurrency` block is untouched. A 6-hour freshness guard skips the rebuild when `generated_at` is still young (a manual dispatch always rebuilds, and an unreadable or missing stamp counts as stale, so a broken file cannot wedge it shut). The checkout takes `main`'s tip on the `workflow_run` path — otherwise it would read the SHA that predates the commit 1.4 just pushed, i.e. the stale record it is here to replace. **On the "fix it with C5" instruction:** that was written while this PR was already open, so the two are landing separately. This half only makes 0.17 *start* more often; while C5 stands, each of those starts can still be killed by a single 502 and write nothing. More attempts is a better lottery than one late attempt a day, but **C5 is what actually unfreezes the record** — it should be the next thing merged, and the record needs one manual dispatch of 0.17 after it lands. **Correction, 4 Sep:** the "Done 3 Sep" claim above was wrong — checked today against the live git history of `.github/workflows/build-workflow-activity.yml`, which shows exactly one commit ever touching that file's trigger (`85dc606`, landing on `main` today as PR **#823**'s squash-merge `0393b3c`, 06:19 UTC). The 3 Sep review was reading the *open PR's* diff and reported it as merged; it was not. This is now genuinely done: the `workflow_run` trigger, the 6-hour freshness guard and the `main`-tip checkout are all on `main` as described. **Not yet observed live** — 0.17 has not fired via the new trigger, because workflow 1.4 has not completed since the merge (20 minutes before this review). Confirm on the next review that 0.17's next run shows `event: workflow_run` rather than `schedule`. |
| **C5** | `[x]` **P1** *(fixed 4 Sep — but the box is ticked on a PR that is **open, not merged**. Per C4's correction below, do not read this as "on `main`" until the PR lands; `git log .../build_workflow_activity.py` settles it.)* | **The record of what your Actions did has been frozen since Monday lunchtime, and this review had to work without it.** One momentary hiccup from GitHub's own API — the kind that fixes itself in seconds — kills the entire job, and because the job writes nothing on the way out, the file just stops updating. It stays frozen until the *next* run happens to get a clean sweep, which on this repo's late crons can be well over a day. The 7-day activity panel on your Data Platform Map is showing Monday's picture, and this log lost its primary evidence source. | Run **11** of 0.17 (2 Sep 11:13 UTC) failed with a single line: `[activity] runs error 9.1 – CI Tests page 1: HTTP Error 502: Bad Gateway`. In `backend/scripts/build_workflow_activity.py`, `_api()` (line 69) wraps one `urllib.request.urlopen` with **no retry at all**, and both call sites — the workflow listing at line 99 and the per-workflow run listing at line 121 — do `except urllib.error.HTTPError: … return 1`, aborting the whole build. The collector makes at least one API call for each of **99** workflows, so it needs ~100 consecutive clean responses to produce any output; one 502 anywhere in that sweep discards the other 98. There is no partial write, so a failed run leaves the previous file in place and the panel silently ages. `generated_at` was `2026-09-01T11:35:16Z` when read at 07:50 on 3 Sep — **44 hours** stale — because run 11 failed and no run has happened since (C4's lateness is why there was no 3 Sep run to recover it). Note this is not a rare coincidence: 502s from the Actions API are routine, and the design turns every one of them into a total loss. **Update, 4 Sep:** run 12 (3 Sep, 11:09 UTC) got a clean sweep and unfroze the record on its own — `generated_at` now reads `2026-09-03T11:09:29Z`, so this is not currently a frozen file, just a late one (C4's problem, now fixed). The code this row is about is unchanged: re-read today, `_api()` in `build_workflow_activity.py` still has no retry and no `errored_workflows` partial-write path, so the exact same freeze will recur the next time a 502 lands mid-sweep — it has simply not happened again yet. | In `backend/scripts/build_workflow_activity.py`, two changes. **(1) Retry transient failures in `_api()`**: on `HTTPError` with status **429, 500, 502, 503, 504** and on `URLError`, retry up to 4 times with exponential backoff (1s, 2s, 4s, 8s), honouring `Retry-After` when present; re-raise anything else (401/403/404 are real and should still fail loudly). **(2) Make one workflow's failure partial, not fatal**: at the per-workflow loop (line 121), stop `return 1`. Instead catch the exception, append the workflow's name to a new `errored_workflows` list alongside the existing `capped_workflows`, and continue to the next workflow. Write the file with whatever was collected, and surface `errored_workflows` in the panel the same way `capped_workflows` already renders its undercount banner — the existing precedent in this very file is *"an undercount that announces itself is recoverable; a quiet one is not."* Keep the hard `return 1` for the **workflow-listing** call at line 99, since with no workflow list there is nothing to collect. After merging, dispatch 0.17 manually once to unfreeze the record. **Done 4 Sep, as proposed — no divergence.** `_api()` now retries **429/500/502/503/504**, `URLError` and a bare read timeout up to 4 times with 1/2/4/8 s backoff, honouring a numeric `Retry-After` capped at 60 s; 401/403/404 and every other status still raise on the first attempt, so a bad token fails fast instead of slowly. The per-workflow run listing no longer returns 1: it records the workflow in a new `errored_workflows` list, keeps the pages that did come back, and moves to the next one — the workflow *listing* keeps its hard failure, since with no list there is nothing to collect. `errored_workflows` ships in the JSON and renders as an amber banner on the Data Platform Map beside the existing `capped_workflows` one. Covered by 18 tests in `backend/tests/test_workflow_activity_resilience.py`, including the exact run-11 scenario (one workflow 502s, the other two collect, the file is still written and names the casualty). The banner was checked by opening the page, not just by `tsc` — it renders as intended. **Two things still to do by hand once this merges:** dispatch 0.17 once to rebuild the record immediately, and confirm on the next review that a run which hits a 502 now writes a file with a non-empty `errored_workflows` rather than writing nothing. |

---

## D. Housekeeping — workflows that no longer earn their keep

| ID | Status | What it means for you (plain language) | Cause | Proposed fix |
|----|--------|----------------------------------------|-------|--------------|
| **D1** | `[ ]` **P3** | 99 workflow files (up from 97 last week), 66 ran in the last recorded week. Most of the other 33 are legitimately idle (monthly, annual, weekly) — but roughly a dozen are finished one-shot tools that will never run again and just add noise every time you scan the list. | `backfill-*` one-shots (b3-conilon, brent-intraday, cepea-conilon, conilon-vitoria, enso-thermocline, fx-snapshots, missing-fields, options-history, sentiment, treasury-yields) and `seed-options-oi.yml`. All still present, and there is still no `_archive/` directory (re-confirmed 3 Sep). **Two exceptions — do not archive these:** `backfill-weather-history.yml` is a live tool (#763 gave it a runner precisely because it had none, and adding a region still requires it), and `backfill-contract-prices.yml` is **D3**. | Move the spent ones to `.github/workflows/_archive/` (GitHub ignores subdirectories), or delete them — git keeps the history either way. Do this once, not repeatedly. **Related:** A6 shows the workflow-number space is now tight enough that two new files collided in one morning; archiving frees numbers as well as attention. |
| **D2** | `[ ]` **P3** | Three near-identical FBX diagnostic probes sit alongside each other; two are superseded. | `0.27 probe-fbx-lanes`, `0.28 probe-fbx-history`, `0.29 probe-fbx-history2` — the second pass exists because the first left holes, and both are now answered by the shipped feature (#809). All three files still present (re-confirmed 3 Sep). | Archive `probe-fbx-history` and `probe-fbx-history2`; keep whichever documents the final answer. |
| **D3** | `[ ]` **P3** | One workflow is currently sitting red in your Actions tab for no reason anyone is tracking. | `Z – Backfill: per-contract prices to 10y (manual)` — `backfill-contract-prices.yml`. Confirmed 3 Sep against the API: only 3 runs ever, the last on **27 Aug ended in failure**, and that is still its latest conclusion. It's a manual tool, so nothing depends on it, but it's the only red thing on the board. Context from #768: the run reports failure *by design* now (it exits 3 when every contract returns no rows), and the suspected cause is barchart.com refusing the runner — which #768 could not diagnose because that sandbox denied barchart.com. The next run was supposed to print the status code. | Either run it once and read the status code it now prints — which is the whole point of the #768 change and takes one dispatch — then fix or archive it accordingly. Leaving it red is the one option that costs something, because it means "red on the board" no longer means anything. |

---

## E. Watch list — not yet a problem, will become one

| ID | Status | What it means for you (plain language) | Detail | When it bites |
|----|--------|----------------------------------------|--------|---------------|
| **E1** | `[x]` | *(Promoted 2 Sep → **B5**; B5 closed 3 Sep.)* The two ICE monthly reports were both 32 days old with the catch window open. | Resolved: both now stamp `2026-08-31`. | Closed. |
| **E2** | `[?]` | The Japan AJCA feed is the closest thing you have to a live alarm — 65 days old against a 70-day limit, at 93% of its allowance. | `ajca` still reads `2026-07-01T00:00:00`, i.e. 65.3 days on 4 Sep against `70 * 24` h in `check-scrapers-freshness.yml`. Was 64.3 d on 3 Sep, so still ageing exactly one day per day — nothing has landed. AJCA is upload-dated and the 70-day limit already accounts for its long lag, so this may still resolve on its own. | Alarm ~**9 Sep** — five days. If nothing lands by then, `1.5` fails and it becomes a real issue rather than a watch. |
| **E3** | `[x]` | *(Closed 2 Sep — resolved, and it was benign.)* `1.13 – ICE Certified Stocks` ran several times a day but its data date appeared stuck on 28 Aug. | It was not stuck: `ice_certified_daily` advanced past the weekend exactly as the T-1 publishing explanation predicted. Confirmed against `health.json`, not inferred from run status. | No action. |
| **E4** | `[?]` | Runs of the ICE stocks scraper are being cancelled — 8 in the last recorded window. Nothing is being lost, but if it ever *does* start losing data the symptom will be a date that stops moving, not a failure. | `scraper-ice-certified-stocks.yml` sets `concurrency: group: scraper-ice-certified-stocks` with `cancel-in-progress: false`. GitHub's model only protects the *running* member, so a queued run is cancelled when a newer one arrives — and 1.13 has two trigger paths (`cron: '35 0 * * 2-6'` plus a `workflow_run` chain off 1.1), so they collide routinely. This is understood and deliberate. **Not re-measured on 3 Sep** — the cancellation count comes from the run record, which is frozen (**C5**). The data side is checked and healthy: `ice_certified_daily` is current. | Watch that `ice_certified_daily` keeps advancing. Becomes a real issue if the data date stops moving for more than 6 days. Re-measure the cancellation count once C5 unfreezes the record. |

**Checked 3 Sep and cleared — with the evidence, per the standing rule that dismissals need proving. Not re-swept today** (budget went to the fresh A7 cluster and confirming C4/C5 instead); `ajca` was spot-checked above and is unchanged in shape, but the other 39 stamps were not re-read this run.

**All 40 scraper freshness stamps are inside their limits.** Checked against the thresholds parsed out of `check-scrapers-freshness.yml` (not eyeballed, and after the detector fix described in the A-section method note): **zero** over threshold. Closest to breaching: `ajca` 64.3 d / 70 d (**92%**, = E2), `cot` and `macro_cot` 9.3 d / 11 d (85%), `brazil_exports` 22.3 d / 45 d (50%), `freight` 4.3 d / 9 d (48%), `psd_coffee` 94.3 d / 210 d (45%, semi-annual and normal). That is why B4 is quiet, and it is a measured result rather than an absence of alarms.

**COT at 85% of its limit is the weekly cycle, not a fault.** `cot`/`macro_cot` read `2026-08-25`, and 2.3 has run daily and succeeded every day since (2 Sep 23:00, 1 Sep ×2, 29, 28, 27, 25 Aug — the last failure was 21 Aug). The CFTC report is weekly: it covers a Tuesday and publishes the following Friday, so between releases the stamp legitimately ages to ~10 days before the next Friday resets it. 25 Aug was a Tuesday; the 1 Sep report lands Friday 4 Sep, one day inside the 11-day threshold. Tight by design rather than drifting — but worth knowing this key sits near its limit every single week, so if the threshold is ever tightened it will cry wolf immediately.

**The `~99 declared / 66 ran` gap is still mostly legitimate.** Per the standing framing, most of it is idle monthly, weekly and annual jobs. The genuinely spent one-shots are enumerated in **D1**, with two explicit exceptions that must not be archived (`backfill-weather-history.yml`, which is a live tool, and `backfill-contract-prices.yml`, which is D3).

**A caveat on this run's own coverage, stated rather than glossed.** Two of the usual evidence sources were degraded. `workflow_activity.json` is 44 h stale (**C5**), so per-workflow failure and cancellation *counts* for 2–3 Sep could not be read from it. And the repo-wide `/actions/runs` listing returned nothing newer than 8 Aug while per-workflow listings returned today's runs correctly — the repo already documents that this endpoint is unreliable (#731: *"1,000 is the hard ceiling GitHub puts on the repo-wide listing however you paginate it"*), so per-workflow listings were used throughout. Consequence: the three gates, the freshness stamps, the sentinel state, 0.17, 9.1, 2.3 and `backfill-contract-prices` were each checked directly and are reported with confidence; **a workflow that failed on 2–3 Sep outside that set would not have been seen by this review.** Fixing C5 restores the sweep.

---

## Suggested order of work

The agent works the **Next up** queue at the top on its own. This section is what *you* do, and it is short by design.

1. **A1** — five minutes in repo settings. It prevents the whole class of problem in section A, and section A now has three instances of it in ten days (A3, A4, A6). It matters more now that something other than you opens pull requests: without required checks, a red check on an agent's PR is only a red icon.
2. **C1** — pick (a) or (b). The agent is blocked on this and will stay blocked until you choose.

Everything else is queued. Your job on those is to read one pull request at a time and merge or reject it.
