# GitHub Actions — open issues log

**Repo:** `loic619/619coffee` · **Opened:** 2026-09-01 · **Last reviewed:** 2026-09-02 (main green; 0.17's cron is late, not on time) · **Last fix-queue run:** 2026-09-02 (B1 — CONAB / Comex probes)
**Evidence window:** 2026-08-25 → 2026-09-01 11:35 UTC (`frontend/public/data/workflow_activity.json`), extended to 2026-09-02 06:40 UTC by live API checks — the run record itself was 19 h stale when read, which is issue **C4** below.

> **How this file works.** Two Claude Code **Routines** keep it current — they
> run on Anthropic's cloud, not in this repo, so there is no workflow here to
> look for. **619coffee — Actions health watch** (daily 13:30 Asia/Saigon) reviews
> what the Actions actually did, ticks off what it can verify, appends new
> findings, updates the *Last reviewed* date, and commits the result to `main`.
> So we never re-derive the same problem twice.
>
> **Status key:** `[ ]` open · `[~]` partly fixed, root cause still open · `[x]` done · `[?]` watch, not yet a problem
>
> Priority is **P1** (silently loses or corrupts data) · **P2** (weakens a safety net) · **P3** (housekeeping).
>
> **This file is also a work queue.** The second routine, **619coffee — Actions
> fix queue** (weekdays 14:30 Asia/Saigon), takes the highest-priority open item,
> fixes it on a branch, ticks the box in the same commit, and opens one pull
> request labelled `auto-fix`. It will not start another until that PR is closed
> or merged — your review sets the pace. Items marked ***(human-only)*** are
> skipped: they need repo settings or a decision from you, which no agent should
> make on your behalf.

---

## Next up

*Rebuilt by the watch routine on every run — if this disagrees with the tables below, the tables win.*

| | Issue | Why it's next |
|---|-------|---------------|
| **In flight** | **B1** · P1 | `auto-fix` PR open (2 Sep): both probe patterns repaired. Nothing else starts until it is merged or closed. |
| **1st** | **C4** · P1 | Make the run record refresh on an event, not a cron GitHub keeps deferring. Until this is fixed, a quiet day in this log is not evidence of a quiet day. |
| **2nd** | **A2** · P2 | Pin ruff. |
| **4th** | **A5** · P2 | Make the smart-quote guard fail loudly when its detector breaks. |
| **5th** | **B2** · P2 | Let the sentinel dispatch its own scraper. |
| **6th** | **B5** · P2 | ICE robusta age-allowance did not refresh with its arabica twin — alarms ~7 Sep. |
| **7th** | **C2** · P2 | Count the rescue dispatches. |
| then | **B6 · D1 · D2 · D3** · P3 | Housekeeping. |

**Waiting on you — the agent will never take these:** **A1** (branch protection; still the highest-value item in this file) · **C1** (pick a polling strategy).

**Not in the queue:** A3, A4, B4, C3, E1, E3 closed · B3, E2, E4 watching.

---

## A. CI gates — the checks that are supposed to stop bad code

**Main is green today.** All three gates were run against `main` at `86af3e5` and each detector was positive-controlled first: **ruff** (0.15.11 *and* 0.15.8) reports "All checks passed" on `backend/`, and a deliberately planted unsorted-import file made it fail with 4 errors, so the pass is real. The **smart-quote** scan is clean under two independent detectors. **pytest** is green in CI — run 2044 on `39059669` reports *1309 passed*; the 18 commits on `main` since then are data-only (`frontend/public/data/**`, every one `[skip ci]`), so no code has gone untested behind them.

| ID | Status | What it means for you (plain language) | Cause | Proposed fix |
|----|--------|----------------------------------------|-------|--------------|
| **A1** | `[ ]` **P1** *(human-only)* | Your gates go red and **nothing stops**. This has now happened twice in one week. Backend Lint was red for three days while **39 pull requests merged** over it (A3); the smart-quote guard was red for five days while **48 merged** (A4). Both went green again by accident, inside PRs aimed at something else. The checks are smoke alarms with no one in the building. | The workflows run on every PR, but they are not **required status checks** in branch protection on `main`. A red result is just a red icon — GitHub still lets the merge through. | In GitHub → Settings → Branches → `main`, mark **9.1 CI Tests**, **9.2 Backend Lint** and **9.3 Smart-quote guard** as required checks. Five minutes, no code. This is the single highest-value item in this file: A3 and A4 are both symptoms of it. |
| **A2** | `[ ]` **P2** | A tool you don't control can turn your build red overnight, for reasons you never chose. | `lint-backend.yml` runs `pip install --quiet ruff` with **no version pin**. Ruff ships new rules frequently; the next release can start flagging code that was fine yesterday. | Pin it: `pip install ruff==0.15.11`, and bump the version deliberately when you want the new rules. *(Verified 2 Sep: 0.15.11 exists on PyPI and passes clean on current `backend/`, so this fix can be applied exactly as written.)* |
| **A3** | `[x]` **P1** | *(Resolved 30 Aug)* Backend Lint failed 63 of 127 runs — not flaky, genuinely broken. | A single unsorted import block (`I001`) in `backend/scraper/probe_port_ids.py`, red on `main` from `bd481087` (27 Aug, #772) through `32862bb6` (29 Aug, #802). Every push run **and** every PR run failed on that one line, and 39 PRs merged over it. **On the introducing commit:** #799's message and a review both attribute it to #778, but the file was *created* by #772 (`git log --diff-filter=A`) already carrying `PORTS, _get, _START_YEAR`, and full-repo ruff flips PASS→FAIL[I001] exactly across `bd481087^ → bd481087`. #778 later reshaped that same line (adding `_HEADERS`); it did not introduce the violation. | Fixed incidentally in `ec1bc457` (#799, 30 Aug) — a one-line reorder folded into an unrelated PR. Closed. A1 is what stops it recurring. |

| **A4** | `[x]` **P1** | *(Resolved 31 Aug — corrected 2 Sep)* The smart-quote guard's 20 failures were **not** the guard doing its job on bad branches. A smart quote was sitting on `main` itself for five days, so the guard went red on innocent branches whose diffs never touched the file. **This is a second instance of A1**, not a non-event. | A typographic quote in `frontend/components/supply/SupplySDTab.tsx` landed with `1afb2660` (#763, 26 Aug) and stayed until `a98835ec` (#803, 31 Aug). Verified by scanning `main` at each commit across the window. | Fixed in #803. Closed. Verify with `git log -S'“' -- frontend/components/supply/SupplySDTab.tsx`. |
| **A5** | `[ ]` **P2** | Your smart-quote guard can break in a way that shows a **green tick**. If it ever stops working, it will not tell you — it will quietly report "no problems found" forever, and you would only discover it the way we discovered A4: weeks later, by accident. | `lint-smart-quotes.yml` runs `grep -rnP '[\x{2018}\x{2019}\x{201C}\x{201D}]' frontend/`. When the shell's locale is not UTF-8, that pattern is not merely unmatched — `grep` **aborts** with *"character code point value in \x{} or \o{} is too large"* and exit code **2**. The step is written as `if grep …; then <fail>; fi`, which treats *any* non-zero exit as "clean" and prints `✓ No smart quotes found`. Reproduced exactly on 2 Sep in the review sandbox (`LC_CTYPE=POSIX` → exit 2 on a file containing a real smart quote; the same command under `LC_ALL=C.UTF-8` matched it correctly). GitHub's current `ubuntu-latest` image sets a UTF-8 locale, so the guard genuinely works today — A4 proves it matched — but nothing in the workflow pins that, and a runner-image change would disarm it silently. | In `lint-smart-quotes.yml`: (1) set `LC_ALL: C.UTF-8` on the step's `env:`; (2) stop inferring the result from `if grep` — capture it explicitly (`set +e; grep -rnP … ; rc=$?; set -e`) and branch on `rc`: `0` = smart quotes found → fail, `1` = clean → pass, **anything else → fail with "detector error"**; (3) add a positive-control step *before* the scan that pipes a known smart quote into the same `grep` invocation and fails the job if it does not match. That last one is the point: a detector that cannot find a planted positive must not be allowed to report "clean". |

**Correction, 2 Sep.** The first version of this file asserted the opposite — that `main` stayed clean and the guard was working as designed, so "don't chase it." That was wrong, and it was wrong for a mechanical reason worth recording: the check used `grep -P '[\x{2018}...]'`, which **errors out** in this environment rather than matching, and the surrounding loop read a non-zero exit as "no hits". Every "clean" it reported was a false negative from a broken detector. Two lessons, both now standing rules for the watch routine: **positive-control any detector before trusting a negative result**, and treat "no action needed" rows as claims to verify, not conclusions — the errors in this file have lived in the dismissals, not the alarms. **Follow-up, same day:** the identical construct is in the shipped workflow, not just in the review script. That is now **A5**.

---

## B. Source detection — how new monthly data gets noticed

| ID | Status | What it means for you (plain language) | Cause | Proposed fix |
|----|--------|----------------------------------------|-------|--------------|
| **B1** | `[~]` **P1** *(probe fix pushed 2 Sep — awaiting a live sweep to confirm)* | **The numbers are current again** — CONAB costs, CONAB safra and MDIC Comex fertilizer all refreshed on 30 Aug and are 2 days old against a 45-day limit, so nothing on your dashboard is stale today. What is still broken is the **detection**: the watcher that is supposed to notice when Brazil publishes has still never once noticed, and has now missed a second month in a row. You are running on the backup timer, and nobody is watching the watcher. | `1.17 – Source Sentinel` should spot publication day and trigger the scraper. For `conab` and `comex`, `data/source_sentinels.json` still shows `last_found: null`, `signal: null` and `confirmed: "2026-07"` — and the alert flags have now rolled forward to `overdue_alerted: "2026-09"` (both) and `blind_alerted: "2026-09"` (conab). The probe patterns for those two sources have never matched. Six other sources (cecafe, dane, ecf, fnc, ucda, vn_customs) detect fine. | **Done:** safety-net crons restored 30 Aug (12th + 20th monthly); data is flowing through them. **Done 2 Sep:** both probes repaired in `backend/scripts/source_sentinel.py`, each against evidence already in the repo. *CONAB* — the probe required an href ending in `.pdf`/`.xls`, but gov.br is a Plone site and neither page carries one: the safra listing links a *folder* per levantamento (the spreadsheet is inside it) and the custos downloads sit behind `/view`. It now keys on what `conab_supply.py` itself keys on — `levantamento-de-cafe` folders, plus coffee-only spreadsheets on the custos *Agrícolas* tab (coffee-only so the FNC false positive is not repeated). *Comex* — every probe raised `SSLError`: `balanca.economia.gov.br` serves an incomplete cert chain, and `comex_fertilizer.py` already downloads that same file with `verify=False` for exactly that reason, which the probe did not. It now matches the scraper's TLS posture and falls back to a 1-byte ranged GET if the host refuses `HEAD`. The `lastmod` probe also gained the `blind` alert `link_hash` already had, so an unreadable file is reported the same day instead of silently at 21 days. **Still open:** confirmation from a live sweep — the fix could not be exercised against the real sites (the agent sandbox has no outbound access to gov.br or balanca), so it rests on the repo's own working scrapers plus unit tests. Watch the next sweep for `conab`/`comex` gaining a non-null `signal`. If either is still blind, the daily blind alert now says so — and *that* is the point at which the "cron-only, detection disabled" decision becomes the right call. |
| **B2** | `[ ]` **P2** | When the sentinel *does* notice something is overdue, it writes a message asking a human to go and run the scraper by hand. That human is you, and it went unread for five days. | The sentinel raised `overdue_alerted` and `blind_alerted` for August and posted *"still undetected 21d past its window — the probe pattern may be broken; run the scraper manually"*. It has no path to actually dispatch the scraper itself. | Give the sentinel the same self-heal path `1.8` already uses: when a source is N days overdue, dispatch its scraper workflow directly instead of asking. The comment in `scraper-monthly-conab.yml` says it best — *"That instruction should not need a human."* |
| **B3** | `[?]` **P3** | Two Vietnam customs sources look half-detected — probably fine, worth one glance. | `vn_customs` and `vn_customs_dest` have a `signal` URL and `confirmed: "2026-08"` but `last_found: null`. Unchanged on 2 Sep. Either by design or a stamping gap. | Confirm which. If by design, no action. |
| **B4** | `[x]` **P2** | *(Working correctly)* `1.5 – Check Data Pipeline Freshness` failed 6 of 7 runs. | Not broken — **that workflow's job is to fail when data is stale.** I replayed it against each day's `health.json`: it fired 26–30 Aug on exactly the three B1 feeds, crossing their 45-day limit, and went green again on 31 Aug once they were refreshed. | No action. This one earned its keep. Treat its failures as messages, not incidents. |
| **B5** | `[ ]` **P2** | One of the two monthly ICE reports quietly did not arrive. The workflow that fetches them ran on 1 Sep and **reported success**, and the arabica report did update — but the robusta one is still July's. It is 33 days old against a 38-day limit, so it turns into a red alert around **7 Sep** if nothing lands. *(Promoted from watch item E1, which has now half-tripped.)* | `1.14 – ICE Monthly Reports` fired 1 Sep at 17:40 UTC (its cron says 14:00 — late again, see C4) and finished green. Afterwards `health.json` reads `ice_arabica_ageing: 2026-08-31` but `ice_robusta_age_allowance: 2026-07-31`. Both carry a 38-day threshold in `check-scrapers-freshness.yml`. The workflow's catch window is `cron: '0 14 1-3,5,8,11 * *'`, so the retries still to come before the 7 Sep breach are **2, 3 and 5 Sep** — the 8th and 11th land *after* it. A guard added in the #7 fix is supposed to keep retrying until *all* monthly feeds carry the target month, so retries should be firing. | On the next run of `1.14`, read the log for the age-allowance fetch and establish which of two things is happening: **(a)** ICE simply has not published the July→August age-allowance file yet and the fetch is 404-ing — in which case no code change is needed, just let the 2/3/5 Sep retries run, and if it is still missing on 5 Sep that is a genuine source outage to raise with a human; or **(b)** the "already captured this month?" guard is skipping the robusta fetch because the arabica half landed — the exact bug #7 fixed for the previous cycle, in which case the guard in `scraper-ice-monthly-reports.yml` has regressed and must require *both* files again. Do not change code before reading that log — the two causes look identical from the run status. |
| **B6** | `[ ]` **P3** | The Cecafe daily scraper failed 4 times in the window, and the error it prints about itself is **wrong**, which would send whoever fixes it chasing the wrong thing. No data was lost — the file stayed inside its 3-day guard and the runs have been green since 31 Aug. | Failures on 26, 29 and 30 (×2) Aug. All three retry attempts in each run died identically: Cecafe served a bot-challenge interstitial (~7,100 chars) and the browser fallback raised `Page.content: Unable to retrieve content because the page is navigating and changing the content`. In `backend/scraper/fetch_cecafe_daily.py` the challenge-polling loop calls `await page.content()` at **line 132** while the Cloudflare challenge is still redirecting; Playwright raises instead of returning HTML, and the exception escapes the loop as `CecafeUnreachable`. The script then prints *"This is the recurring connect-timeout pattern"* — it is not a connect timeout, and nothing timed out. | Two small changes in `backend/scraper/fetch_cecafe_daily.py`: (1) wrap the in-loop `await page.content()` (line 132) in `try/except` and, on a navigation-in-progress error, treat it as *"challenge still up"* — sleep `poll_ms` and continue the loop instead of aborting the whole run; apply the same guard to the settle read at line 139, retrying once after `await page.wait_for_load_state("domcontentloaded")`. (2) Correct the message so it names the bot challenge rather than a connect timeout. |

---

## C. Scheduling reliability — whether crons actually fire

| ID | Status | What it means for you (plain language) | Cause | Proposed fix |
|----|--------|----------------------------------------|-------|--------------|
| **C1** | `[ ]` **P1** *(human-only — pick an option first)* | Your live-quote poller is scheduled to run ~360 times a week. It actually got **45 scheduled runs — about 12%**. The feed only stayed alive because a rescue mechanism fired **241 emergency runs** to cover the gap. It works, but you are running on the backup generator full-time and the main power has been out for weeks. | GitHub Actions silently throttles high-frequency crons on this repo (documented twice before in `poll-acaphe-quotes.yml`: June and August). The `1.8` freshness checker compensates by dispatching the poller whenever quotes go stale. | Two options, pick one: **(a)** accept the rescue path as the primary mechanism and rewrite the cron down to a low, reliable heartbeat — stop pretending the 15-minute schedule works; **(b)** move the polling loop to a single long-running job (one run that sleeps and polls) instead of many short cron ticks, which GitHub throttles far less. Either way, the current setup hides its own failure. |
| **C2** | `[ ]` **P2** | That rescue mechanism is a single point of failure holding up your most time-sensitive feed, and if it breaks the failure is silent. | `check-live-quotes.yml` self-heals by calling the GitHub API (`actions/workflows/poll-acaphe-quotes.yml/dispatches`), which needs `actions:write`. If the token, permission or API call breaks, the poller drops to its real ~12% cron rate and nothing announces it. | Add a cheap tripwire: alert if scheduled-run count over 24h falls below a floor, or if `1.8` issues more than N rescues in a day. Right now nobody is counting the rescues. |
| **C3** | `[x]` **P3** | *(Closed 2 Sep — solved by rescheduling, not by code.)* The daily review used to read a run-record file that was ~19 hours old, so it could not see the previous afternoon. It now reads one that is twenty minutes old. | The 08:00 Cowork debrief this row was written for no longer exists. Its replacement, the **Actions health watch** routine, runs at 13:30 Asia/Saigon = 06:20 UTC — *after* workflow 0.17 rebuilds `workflow_activity.json` at 06:10 UTC. | No action. The proposed `00:40 UTC` cron on `build-workflow-activity.yml` is **not** needed; do not add it. |
| **C4** | `[ ]` **P1** | **This one is about trusting this file.** C3 above was closed on the belief that the run record is rebuilt at 06:10 UTC, just before the daily review reads it. That belief is wrong: the rebuild almost never happens at 06:10. When today's review ran at 06:40 UTC the record was still **19 hours old** and no rebuild had happened at all that morning. So when this log says "nothing failed", it is reporting on a window that may be missing the last day — and the two worst incidents in this file (A3, A4) were both things that sat unnoticed. A quiet report is not currently proof of a quiet day. | `build-workflow-activity.yml` (workflow **0.17**) declares `cron: '10 6 * * *'`. Its actual scheduled start times: 26 Aug **07:06**, 27 Aug **17:36**, 28 Aug **18:37**, 29 Aug **12:39**, 30 Aug **11:51**, 31 Aug **13:42**, 1 Sep **11:35** — between 56 minutes and **12 h 28 m** late, never near 06:10. On 2 Sep there was no run by 06:40 and the committed file still read `generated_at: 2026-09-01T11:35:16Z`. This is the same repo-wide throttling as C1, and it is already documented in the repo: `scraper-ice-certified-stocks.yml` carries the comment *"GH cron on this repo fires 4-5h LATE systematically (verified 2026-06-10 against run history)"*. It also explains B5's 1.14 firing at 17:40 against a 14:00 cron. | Stop depending on the cron's timing. In `build-workflow-activity.yml`, add an event trigger alongside the existing schedule: `workflow_run:` on `workflows: ["1.4 – Export and Publish Static JSON"]`, `types: [completed]`. 1.4 runs several times a day at the end of the data cycle, so the record is then never more than one export cycle old whenever the review reads it. Keep the cron as a fallback and keep the existing `concurrency` block. To stop it rebuilding six times a day, add a job-level guard that exits early when `workflow_activity.json`'s `generated_at` is under ~6 hours old — the run costs ~90 s, so the guard is about tidiness, not cost. **Do not** add the `00:40 UTC` cron that C3 proposed: the problem is that crons on this repo fire late, so a second cron inherits the same defect. |

---

## D. Housekeeping — workflows that no longer earn their keep

| ID | Status | What it means for you (plain language) | Cause | Proposed fix |
|----|--------|----------------------------------------|-------|--------------|
| **D1** | `[ ]` **P3** | 97 workflow files, 66 ran this week. Most of the other 31 are legitimately idle (monthly, annual, weekly) — but roughly a dozen are finished one-shot tools that will never run again and just add noise every time you scan the list. | `backfill-*` migrations (b3-conilon, cepea-conilon, conilon-vitoria, enso-thermocline, fx-snapshots, missing-fields, options-history, sentiment, brent-intraday) and `seed-options-oi.yml` were all one-time jobs that have completed. All still present, and there is still no `_archive/` directory (re-confirmed 2 Sep). | Move the spent ones to `.github/workflows/_archive/` (GitHub ignores subdirectories), or delete them — git keeps the history either way. Do this once, not repeatedly. |
| **D2** | `[ ]` **P3** | Three near-identical FBX diagnostic probes sit alongside each other; two are superseded. | `0.27 probe-fbx-lanes`, `0.28 probe-fbx-history`, `0.29 probe-fbx-history2` — the second pass exists because the first left holes, and both are now answered by the shipped feature (#809). All three files still present (re-confirmed 2 Sep). | Archive `probe-fbx-history` and `probe-fbx-history2`; keep whichever documents the final answer. |
| **D3** | `[ ]` **P3** | One workflow is currently sitting red in your Actions tab for no reason anyone is tracking. | `Z – Backfill: per-contract prices to 10y (manual)` — `backfill-contract-prices.yml`, last run 27 Aug, **ended in failure** (still the last conclusion as of 2 Sep). It's a manual tool, so nothing depends on it, but it's the only red thing on the board. | Either fix it or archive it, so "red on the board" means something again. |

---

## E. Watch list — not yet a problem, will become one

| ID | Status | What it means for you (plain language) | Detail | When it bites |
|----|--------|----------------------------------------|--------|---------------|
| **E1** | `[x]` | *(Promoted 2 Sep → **B5**.)* The two ICE monthly reports were both 32 days old with the catch window open. | It has now half-tripped: arabica ageing refreshed to 2026-08-31, robusta age-allowance did not and is still 2026-07-31. That is no longer a watch — it is a workflow reporting success while one of its two outputs stands still. | Tracked as B5. |
| **E2** | `[?]` | The Japan AJCA feed is 63 days old against a 70-day limit. | Upload-dated source; the limit already accounts for AJCA's long lag, so this may resolve on its own. Was 62 days at the last review, so it is ageing normally rather than newly stuck. | Alarm ~9 Sep. |
| **E3** | `[x]` | *(Closed 2 Sep — resolved, and it was benign.)* `1.13 – ICE Certified Stocks` ran several times a day but its data date appeared stuck on 28 Aug. | It was not stuck: `ice_certified_daily` now reads **2026-08-31**, 2.3 days old against a 6-day threshold. The date has advanced past the weekend exactly as the T-1 publishing explanation predicted. Confirmed against `health.json`, not inferred from run status. | No action. |
| **E4** | `[?]` | Runs of the ICE stocks scraper are being cancelled — 8 of them in the window, and the same pattern again this morning. Nothing is being lost today, but it is worth knowing this is happening, because if it ever *does* start losing data the symptom will be a date that stops moving, not a failure. | `scraper-ice-certified-stocks.yml` sets `concurrency: group: scraper-ice-certified-stocks` with `cancel-in-progress: false`. GitHub's model only protects the *running* member, so a queued run is cancelled when a newer one arrives — and 1.13 has two trigger paths (`cron: '35 0 * * 2-6'` plus a `workflow_run` chain off 1.1), so they collide routinely. This is understood and deliberate. On 2 Sep at 06:34: run 356 in progress since 04:58, run 357 cancelled, run 358 pending. | Watch two things: that `ice_certified_daily` keeps advancing (it is, see E3), and run 356's ~1 h 34 m runtime, which is long for this scraper and worth one glance if it repeats. Becomes a real issue if the data date stops moving for more than 6 days. |

**Checked this run and cleared — with the evidence, per the standing rule that dismissals need proving.**
**1.11 Port Activity (4 failures of 6)** — not an ongoing fault. All four were `workflow_dispatch` runs on 27 Aug, an in-session debugging sequence (#769 → #770 → #771 → #779); the final dispatch, run 22, succeeded, and the last *scheduled* run (26 Aug) also succeeded. `port_activity` is 5.8 days old against a 10-day threshold. *(Its weekly `cron: '17 6 * * 3'` had not fired by 06:40 on Wed 2 Sep — consistent with C4, not a separate fault.)*
**Every other freshness stamp** — all 39 scraper stamps in `health.json` were checked against the per-source thresholds in `check-scrapers-freshness.yml`, and **none is over its limit**. The oldest are `psd_coffee` 93 d (limit 210, semi-annual), `ajca` 63 d (limit 70, E2), `ice_robusta_age_allowance` 33 d (limit 38, **B5**), `brazil_exports` 21 d and `ecf` 20 d (limits 45 and 90). That is why B4 is quiet, and it is a measured result rather than an absence of alarms.

---

## Suggested order of work

The agent works the **Next up** queue at the top on its own. This section is what *you* do, and it is short by design.

1. **A1** — five minutes in repo settings. It prevents the whole class of problem in section A, and it matters more now that something other than you opens pull requests: without required checks, a red check on an agent's PR is only a red icon.
2. **C1** — pick (a) or (b). The agent is blocked on this and will stay blocked until you choose.

Everything else is queued. Your job on those is to read one pull request at a time and merge or reject it.
