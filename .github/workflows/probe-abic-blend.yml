name: "Probe: ABIC blend composition (temporary)"

on:
  workflow_dispatch:

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: pip install --quiet requests pypdf
      - name: Probe
        working-directory: backend
        run: PYTHONPATH=. python -m scraper.probe_abic_blend
