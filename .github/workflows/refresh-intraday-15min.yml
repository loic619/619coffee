name: "1.98 – Refresh Intraday KC/RC 15min (daily)"

# Daily incremental refresh of the 15-min intraday points (RM + KC) so the
# open-direction model's two intraday features (kc_after_rc_diff,
# rc_overnight_gap) stay current and the model's scored `as_of` doesn't freeze.
#
# Unlike the deep backfill (1.97, ~5.7y from 59 contracts, manual), this fetches
# only the few NEAREST contracts, volume-stitches them with the SAME logic as
# the backfill, extracts the recent days and MERGES them into the existing file
# (recent dates overwrite, older history preserved). See
# backend/scraper/refresh_intraday_kc_rc.py.
#
# Scheduled at 20:13 UTC Mon-Fri: after both markets close (KC 18:30 London =
# 17:30 UTC summer / 18:30 UTC winter) and before the 21:30 UTC quant run (1.9),
# so the model picks up the fresh intraday the same day. Same-day official
# settles aren't in the archive yet at this hour — that's fine, the model
# doesn't use the settle; the next day's refresh fills it in.

on:
  schedule:
    - cron: "13 20 * * 1-5"
  workflow_dispatch:

# Own lane — NOT the shared `data-commit` group.
#
# 25 workflows used to sit in that one group. GitHub's concurrency model only
# protects the RUNNING member: `cancel-in-progress: false` keeps the in-progress
# run alive, but the queue holds exactly ONE pending run, so whenever a third
# member arrives the one already waiting is CANCELLED outright — its scraped
# data thrown away before it ever ran. Observed 2026-08-14 on 1.3 Daily OI:
# three cancelled runs in one morning, one of them killed after 31 minutes.
# 1.16 hit the same thing and was given its own lane in August for exactly this
# reason; this generalises that fix.
#
# Serialising the push is what the shared group was really buying, and that is
# now handled properly by .github/actions/commit-data, which re-syncs with
# origin/main and retries with backoff on every attempt.
concurrency:
  group: refresh-intraday-15min
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install deps + chromium
        run: |
          pip install --quiet playwright
          sudo rm -f /etc/apt/sources.list.d/*microsoft* /etc/apt/sources.list.d/*azure* || true
          playwright install chromium --with-deps || playwright install chromium
      - name: Refresh 15-min intraday (incremental, 3 attempts)
        working-directory: backend
        # The intraday scrape drives the open-direction model's two intraday
        # features; a one-off Playwright/network hiccup would otherwise freeze
        # the model's as_of. Retry with short backoff (job timeout is 15m).
        run: |
          for attempt in 1 2 3; do
            python -m scraper.refresh_intraday_kc_rc && break
            [ $attempt -lt 3 ] && echo "Attempt $attempt failed, retrying in 30s..." && sleep 30 || exit 1
          done
      - name: Commit intraday refresh
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add frontend/public/data/intraday_kc_rc_15min.json
          git diff --cached --quiet \
            && echo "No change." \
            || (git commit -m "data: refresh intraday KC/RC 15min [skip ci]" \
                && git pull --rebase --autostash --strategy-option=theirs origin main \
                && git push)

      - name: Notify on failure
        if: failure()
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -d "chat_id=${TELEGRAM_CHAT_ID}" \
            -d "text=🚨 ${{ github.workflow }} failed — $(date -u +'%Y-%m-%d %H:%M UTC') — https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}" || true
