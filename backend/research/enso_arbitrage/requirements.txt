# Analysis stack for backend/research/enso_arbitrage — isolated from the
# scraper's requirements on purpose: nothing in the export pipeline imports
# this package, and CI's pytest job does not collect its tests.
numpy>=1.26,<3
pandas>=2.1,<3
scipy>=1.11,<2
statsmodels>=0.14,<0.15
matplotlib>=3.8,<4
openpyxl>=3.1,<4
xlrd==1.2.0
pytest>=8
