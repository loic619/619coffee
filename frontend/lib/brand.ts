/**
 * The product has one name. It used to have three — "Coffee Intel Map" on the
 * gate and the PDF header, "Coffee Intel Dashboard" in the browser tab, and
 * "619coffee" on the repo and the domain — which is what a visitor sees when
 * nothing was decided. Everything user-facing reads from here; change it once.
 */
export const PRODUCT_NAME = "619coffee";

/** One line under the name, where there is room for it. */
export const PRODUCT_TAGLINE = "Coffee market intelligence";
